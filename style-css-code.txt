* { box-sizing: border-box; }
html { scroll-behavior: auto; }
body {
  margin:0;
  min-height:100vh;
  overflow-x:hidden;
  font-family: "Comic Sans MS", system-ui, sans-serif;
  background:
    radial-gradient(circle at 15% 20%, rgba(255,0,120,.3), transparent 20%),
    radial-gradient(circle at 80% 10%, rgba(0,255,120,.24), transparent 22%),
    linear-gradient(135deg,#260016,#111,#273300);
  color:#fff9d7;
  cursor:none;
}
@keyframes illegalTilt {
  0%,100% { transform: rotate(0deg); }
  35% { transform: rotate(.35deg); }
  65% { transform: rotate(-.45deg); }
}
body.chaosMode { animation: pagePanic .8s infinite linear; }
@keyframes pagePanic {
  0% { transform: rotate(.35deg) translate(1px,-1px); }
  50% { transform: rotate(-.35deg) translate(-1px,1px); }
  100% { transform: rotate(.35deg) translate(1px,-1px); }
}
.screen {
  display:none;
  min-height:100vh;
  padding:30px;
  position:relative;
}
.screen.active { display:block; }
h1,h2 { text-shadow: 3px 3px #000, -2px 0 #ff006a; }
h1 { font-size: clamp(2rem, 5vw, 4.5rem); margin: 5px 0 15px; }
h2 { font-size: clamp(1.6rem, 4vw, 3rem); }
button, .btnish {
  border: 3px solid #111;
  background: linear-gradient(90deg,#eaff00,#ff5b00,#00ff9d);
  color:#190015;
  font-weight:900;
  padding:12px 18px;
  border-radius: 14px;
  cursor:none;
  box-shadow: 0 7px 0 #000;
  transition: transform .14s ease, left .25s ease, top .25s ease;
  position: relative;
  animation: heartBeat 1.7s infinite;
}
@keyframes heartBeat {
  0%,100% { scale:1; }
  50% { scale:1.06; }
}
button:hover { filter:hue-rotate(110deg); }
input, select, textarea {
  width:100%;
  padding:13px;
  border-radius:10px;
  border:3px solid #d7ff00;
  background:#140014;
  color:#f7ffba;
  font-size:1rem;
  margin:7px 0 14px;
}
label { font-weight:900; color:#f5ff68; }
small { color:#ffb3d5; }
.card {
  background:rgba(0,0,0,.6);
  border: 3px solid rgba(255,255,255,.25);
  border-radius: 24px;
  padding:20px;
  box-shadow: 0 0 25px rgba(255,0,120,.3);
}
.login-grid { display:grid; grid-template-columns: minmax(280px,520px) 1fr; gap:28px; align-items:start; }
.germ {
  position:absolute; font-size:1.8rem; animation:floatGerm 5s infinite ease-in-out;
  opacity:.8; pointer-events:none;
}
@keyframes floatGerm {
  0%,100% { transform:translate(0,0) rotate(0); }
  50% { transform:translate(40px,-25px) rotate(35deg); }
}
#loginBox { background:linear-gradient(160deg,rgba(71,50,0,.8),rgba(0,45,14,.8)); }
#loginBox::after {
  content:"mystery stain";
  position:absolute;
}
.strength { height:18px; border:2px solid #fff; border-radius:20px; overflow:hidden; margin-bottom:4px; }
.strength span { display:block; height:100%; width:10%; background:#ff0033; transition:width .2s; }
#cursor {
  position:fixed; z-index:999999; pointer-events:none; transform-origin:center;
  font-size:30px; left:0; top:0; rotate:0deg; transition: rotate .25s linear;
  filter: drop-shadow(0 0 6px #fff);
}
.cloneCursor { position:fixed; z-index:999998; pointer-events:none; font-size:26px; opacity:.65; }
#toastZone, #errorZone, #adZone { position:fixed; z-index:9000; pointer-events:none; }
#toastZone { right:15px; bottom:15px; width:min(340px,90vw); }
#errorZone { left:12px; top:12px; width:min(360px,90vw); }
#adZone { inset:0; }
.toast,.sysError {
  pointer-events:auto;
  background:#111;
  border:3px solid #ffef00;
  color:#fff;
  padding:12px;
  margin:8px;
  border-radius:12px;
  box-shadow:0 0 20px #000;
  animation:popIn .25s ease;
}
.sysError { background:#001a80; border-color:#fff; font-family:monospace; }
@keyframes popIn { from { transform:scale(.5) rotate(8deg); opacity:0; } to { transform:scale(1); opacity:1; } }
.fake-ad {
  pointer-events:auto;
  position:fixed;
  width:230px;
  padding:12px;
  background:#fff200;
  color:#000;
  border:4px solid #f00;
  font-weight:900;
  z-index:9100;
  box-shadow:8px 8px #000;
}
.ad-close {
  position:absolute; right:2px; top:2px; width:13px; height:13px; padding:0; font-size:8px; border-radius:0;
  animation:none;
}
.menu-drift { animation: none; }
@keyframes drift { 0%,100% { transform:translateX(0); } 50% { transform:translateX(22px); } }
.categories { display:flex; overflow-x:auto; gap:12px; padding:12px 0; scrollbar-width:thin; }
.cat { min-width:160px; background:#2b0031; border:2px solid #fffb00; padding:10px; border-radius:20px; text-align:center; }
.food-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(230px,1fr)); gap:18px; }
.food { position:relative; min-height:310px; overflow:hidden; }
.food:hover { animation:shake .18s infinite; }
@keyframes shake {
  0% { transform:translate(0,0) rotate(0); }
  25% { transform:translate(5px,-4px) rotate(2deg); }
  50% { transform:translate(-4px,3px) rotate(-2deg); }
  75% { transform:translate(3px,5px) rotate(1deg); }
  100% { transform:translate(0,0) rotate(0); }
}
.food .pic { font-size:5rem; text-align:center; background:#101010; border-radius:18px; padding:18px; margin-bottom:8px; }
.badge { position:absolute; top:10px; right:10px; background:#ff2d00; color:#fff; padding:5px 8px; border-radius:20px; font-size:.8rem; transform:rotate(8deg); }
.price { font-size:1.2rem; font-weight:900; color:#78ff00; }
.qty { display:flex; gap:8px; align-items:center; margin:10px 0; }
.cartbar {
  position:sticky; top:0; z-index:50; background:#000; border:3px solid #ff0; border-radius:18px; padding:12px; margin-bottom:14px;
}
.loadingOverlay,.blueScreen,.finalCrash {
  position:fixed; inset:0; display:none; place-items:center; z-index:99999;
  background:#070707; color:#fff; text-align:center; padding:30px;
}
.loadingOverlay.active,.blueScreen.active,.finalCrash.active { display:grid; }
.bar { width:min(520px,90vw); height:30px; border:3px solid #fff; margin:20px auto; border-radius:20px; overflow:hidden; }
#loadFill { height:100%; width:12%; background:linear-gradient(90deg,#ff0,#f0f,#0ff); transition:width .25s; }
.checkout-grid { display:grid; grid-template-columns:1.2fr .8fr; gap:20px; }
#placeOrder { position:fixed; right:8px; bottom:8px; width:32px; height:22px; padding:0; font-size:6px; border-radius:4px; z-index:100; }
#sacrifice { width:auto; transform:scale(.65); }
.dialogStack { position:fixed; inset:0; display:none; place-items:center; background:rgba(0,0,0,.55); z-index:20000; }
.dialogStack.active { display:grid; }
.dialog { background:#fff; color:#000; border:5px solid red; padding:20px; border-radius:20px; max-width:420px; }
canvas { width:100%; height:160px; background:#fff; border:5px dashed #000; border-radius:14px; cursor:crosshair; }
.chatbot { position:fixed; right:16px; top:130px; width:250px; background:#2b0031; border:3px solid #0ff; z-index:5000; padding:10px; border-radius:20px; }
.chatbotBody { height:90px; overflow:auto; font-size:.9rem; background:#000; padding:8px; margin:8px 0; }
.bigCancel { font-size:1.4rem; background:#00ff5a; padding:18px 30px; }
.tinyContinue { font-size:8px; opacity:.55; padding:4px 6px; }
#success { text-align:center; }
.confetti { position:fixed; top:-20px; font-size:24px; animation:fall 2.4s linear forwards; z-index:9999; }
@keyframes fall { to { transform:translateY(110vh) rotate(720deg); } }
.vibrate { animation:textVibe .15s infinite; display:inline-block; }
@keyframes textVibe { 0%,100% { transform:translateX(0); } 50% { transform:translateX(2px); } }
@media (max-width:800px) { .login-grid,.checkout-grid { grid-template-columns:1fr; } .chatbot{position:static;width:auto;margin:12px 0;} }

.topCountdown {
  position: sticky;
  top: 0;
  z-index: 8000;
  width: 100%;
  padding: 10px 14px;
  background: linear-gradient(90deg,#fffb00,#ff4d00,#00ff9d);
  color: #160014;
  border-bottom: 4px solid #000;
  font-weight: 900;
  text-align: center;
  font-size: clamp(1rem, 2.5vw, 1.45rem);
  box-shadow: 0 6px 0 #000;
}
.topCountdown b {
  display: inline-block;
  min-width: 34px;
  padding: 2px 8px;
  margin: 0 4px;
  background: #000;
  color: #fffb00;
  border-radius: 10px;
}
body.menuNightmare {
  background:
    radial-gradient(circle at 20% 10%, rgba(0,255,255,.25), transparent 18%),
    radial-gradient(circle at 80% 70%, rgba(255,255,0,.25), transparent 22%),
    linear-gradient(135deg,#001c27,#230030,#111);
}
body.menuNightmare .food {
  border-color: #00fff2;
  box-shadow: 0 0 24px rgba(0,255,242,.35);
}
body.menuNightmare h1::after {
  content: "  — different nightmare unlocked";
  font-size: .35em;
  color: #00fff2;
}


.fakeOrderBtn{
  position:absolute;
  padding:12px 18px;
  border-radius:14px;
  border:3px solid #000;
  font-weight:900;
  cursor:none;
  background:linear-gradient(90deg,#ff0,#ff5b00,#00ff9d);
  color:#190015;
  box-shadow:0 6px 0 #000;
}
.fakeOrderBtn:hover{
  transform:scale(1.06) rotate(-2deg);
}
.realOrderBtn{
  background:linear-gradient(90deg,#00ff9d,#00e1ff,#ff0)!important;
}




#finalVideoOverlay{
  position:fixed;
  inset:0;
  background:#000;
  z-index:999999;
  display:none;
  align-items:center;
  justify-content:center;
  flex-direction:column;
}
#finalVideoOverlay.active{
  display:flex;
}
#finalMemeVideo{
  width:min(90vw,900px);
  max-height:75vh;
  border:6px solid #fff200;
  border-radius:20px;
  box-shadow:0 0 40px rgba(255,255,0,.5);
}
#videoCaption{
  margin-top:16px;
  font-size:clamp(1.5rem,4vw,3rem);
  font-weight:900;
  color:#fff200;
  text-shadow:4px 4px #000;
  animation: captionPulse 1s infinite;
}
@keyframes captionPulse{
  0%,100%{transform:scale(1)}
  50%{transform:scale(1.08)}
}